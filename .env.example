# Beam node environment (orchestrator, worker, validator)
# Copy to `.env` and adjust. Never commit `.env`.
#
# BeamCore endpoints:
# CORE_SERVER_URL      -> BeamCore HTTP API
# ORCH_GATEWAY_URL     -> BeamCore NATS orchestrator control endpoint
# WORKER_GATEWAY_URL   -> orchestrator-owned worker gateway

# =============================================================================
# MAINNET
# =============================================================================
CORE_SERVER_URL=https://beamcore.b1m.ai
ORCH_GATEWAY_URL=tls://orch-gateway.b1m.ai:4222
WORKER_GATEWAY_URL=https://your-orchestrator-worker-gateway.example

SUBTENSOR_NETWORK=finney
NETUID=105

# =============================================================================
# WORKER
# =============================================================================

# =============================================================================
# ORCHESTRATOR
# =============================================================================
WALLET_NAME=orchestrator
WALLET_HOTKEY=default
# WALLET_PATH=~/.bittensor/wallets
# READY=false

# API_PORT=8000
# LOG_LEVEL=INFO

# =============================================================================
# VALIDATOR
# =============================================================================
BEAM_VALIDATOR_WALLET_NAME=validator
BEAM_VALIDATOR_WALLET_HOTKEY=default
# BEAM_VALIDATOR_WALLET_PATH=~/.bittensor/wallets

BEAM_VALIDATOR_CORE_SERVER_URL=https://beamcore.b1m.ai

# BEAM_VALIDATOR_PORT=8093
# BEAM_VALIDATOR_LOG_LEVEL=INFO
